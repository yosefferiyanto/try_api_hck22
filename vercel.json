{
    "devCommand": "uvicorn try_api:app --host 0.0.0.0 --port 3000",
    "builds": [
      {
        "src": "try_api.py",
        "use": "@vercel/python"
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "try_api.py"
      }
    ]
  }